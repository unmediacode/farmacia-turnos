# Base de datos (local SQLite por defecto)
# Para Turso, define:
# TURSO_DATABASE_URL="libsql://<org>-<db>.turso.io"
# TURSO_AUTH_TOKEN="<token>"

# Autenticación opcional
# Si es true, las operaciones de escritura (POST/PUT/DELETE) requieren JWT válido de Supabase
REQUIRE_AUTH=false
# Uno de los dos métodos:
# 1) Usar JWKS (recomendado):
# SUPABASE_JWKS_URL="https://<project>.supabase.co/auth/v1/keys"
# 2) Usar JWT secret simétrica (solo si la conoces y confías en el entorno):
# SUPABASE_JWT_SECRET="<your-supabase-jwt-secret>"
